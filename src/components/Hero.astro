<section id="inicio" class="relative min-h-screen flex items-center pt-32 pb-24 overflow-hidden">
  <!-- Fondo degradado: navy oscuro (izq) -> azul brillante (der) -->
  <div class="absolute inset-0 z-0 pointer-events-none">
    <div class="absolute inset-0 bg-gradient-to-br from-[#04101f] via-[#06203f] to-[#0a4d8f]"></div>

    <!-- Patrón radial sutil -->
    <div
      class="absolute inset-0 opacity-[0.12]"
      style="background-image: repeating-radial-gradient(circle at center, transparent 0, transparent 40px, #334155 41px, #334155 42px);"
    ></div>

    <!-- Glow de acento -->
    <div class="absolute top-1/3 right-0 w-[700px] h-[500px] bg-cyan-500/20 rounded-full blur-[160px]"></div>
    <div class="absolute bottom-0 left-0 w-[600px] h-[400px] bg-emerald-500/10 rounded-full blur-[160px]"></div>

    <!-- Difuminado hacia el contenido siguiente -->
    <div class="absolute inset-x-0 bottom-0 h-40 bg-gradient-to-b from-transparent to-[#050810]"></div>
  </div>

  <div class="container mx-auto px-6 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">

      <!-- Columna izquierda: Titular -->
      <h1 class="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-black tracking-tighter leading-[1.05]">
        <span class="block text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 via-cyan-300 to-emerald-400 bg-[length:200%_auto] animate-gradient">
          Tener IA no basta.
        </span>
        <span class="block text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-emerald-300 to-cyan-400 bg-[length:200%_auto] animate-gradient animate-gradient-delay">
          Hacerla trabajar lo cambia todo.
        </span>
      </h1>

      <!-- Columna derecha: Apoyo + CTAs -->
      <div class="flex flex-col gap-8 lg:pt-2">
        <p class="text-slate-200/90 text-base md:text-lg leading-relaxed max-w-xl">
          La mayoría de las empresas habla de inteligencia artificial. Pocas tienen las
          <span class="text-white font-semibold">automatizaciones</span>,
          <span class="text-white font-semibold">integraciones</span> y los
          <span class="text-white font-semibold">sistemas a medida</span> para usarla de verdad.
          En SurCode construimos esa base, para que la IA no solo suene bien: trabaje por ti.
        </p>

        <div class="flex flex-col sm:flex-row gap-4">
          <a
            href="#contacto"
            class="group relative inline-flex items-center justify-center px-8 h-14 rounded-full bg-emerald-400 text-[#04101f] font-black uppercase tracking-widest text-xs transition-all hover:bg-emerald-300 hover:scale-105 shadow-[0_10px_40px_rgba(52,211,153,0.35)]"
          >
            Solicitar Propuesta
          </a>
          <a
            href="#servicios"
            class="inline-flex items-center justify-center px-8 h-14 rounded-full border-2 border-emerald-400 text-emerald-300 font-black uppercase tracking-widest text-xs transition-all hover:bg-emerald-400/10 hover:text-white"
          >
            Ver Servicios
          </a>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  @keyframes shine-gradient { to { background-position: 200% center; } }
  .animate-gradient { animation: shine-gradient 5s linear infinite; }
  /* Desfase para que la segunda línea brille en contratiempo con la primera */
  .animate-gradient-delay { animation-delay: -2.5s; }
</style>
